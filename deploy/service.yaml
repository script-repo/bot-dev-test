apiVersion: v1
kind: Service
metadata:
  name: bot-dev-test
  namespace: bot-dev-test
  labels:
    app: bot-dev-test
spec:
  type: NodePort
  selector:
    app: bot-dev-test
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
