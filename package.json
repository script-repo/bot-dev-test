{
  "name": "bot-dev-test",
  "version": "1.0.0",
  "private": true,
  "description": "Simple web app for Dev Lead harness test (GHCR + Flux/MicroK8s)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
